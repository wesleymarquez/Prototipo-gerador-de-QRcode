🚀 Guia Completo: Criar Aplicação e Subir no GitHub pelo VS Code

🔧 1. Configuração Inicial (primeira vez)

Instale o Git no seu computador.

Configure seu usuário global:

git config --global user.name "Seu Nome"
git config --global user.email "seuemail@exemplo.com"

Verifique se está tudo certo:

git config --list

📂 2. Criar Projeto Local

Crie uma pasta para sua aplicação:

mkdir minha-aplicacao
cd minha-aplicacao

Inicie o repositório Git:

git init

📝 3. Criar Arquivos da Aplicação

Abra o VS Code e crie seus arquivos (ex.: index.html, app.js, etc).

Salve tudo dentro da pasta do projeto.

✅ 4. Verificar Status

git status

➕ 5. Adicionar Arquivos

Todos os arquivos:

git add .

Arquivo específico:

git add nome.ext

💬 6. Fazer Commit

git commit -m "Primeiro commit: estrutura inicial da aplicação"

🌐 7. Criar Repositório no GitHub

Vá até o GitHub, crie um novo repositório sem README (para evitar conflito).

Copie a URL do repositório.

🔗 8. Conectar Repositório Local ao GitHub

git remote add origin https://github.com/SEU_USUARIO/NOME_DO_REPO.git
git remote -v

📤 9. Enviar para o GitHub

Branch padrão hoje é main:

git branch -M main
git push -u origin main

🔄 10. Atualizar Projeto (quando fizer mudanças)

git add .
git commit -m "Descrição das alterações"
git push

📥 11. Puxar Atualizações (se alguém mexeu no repo)

git pull origin main

⚡ Dicas Extras

Criar nova branch para features

git checkout -b nome-da-feature

Usar aliases para agilizar

git config --global alias.st status
git config --global alias.ci "commit -m"
git config --global alias.co checkout
git config --global alias.br branch

👉 Exemplo: git st em vez de git status.
